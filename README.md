#HRMS Lite

A full‑stack HRMS (Human Resource Management System) built with Django + MongoDB (MongoEngine) for backend APIs and React.js for frontend, deployed using PM2 and ecosystem configuration for production-grade process management.
